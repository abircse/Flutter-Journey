import 'package:flutter/material.dart';

class StatusTabSceeen extends StatelessWidget {
  const StatusTabSceeen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(
        child: Text("Welcome From Status tab"),
      ),
    );
  }
}